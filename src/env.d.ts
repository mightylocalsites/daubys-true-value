/// <reference types="astro/client" />
/// <reference types="@mightylocalsites/astro-site/env" />

// Env var typings (MIGHTY_API_BASE_URL / MIGHTY_SITE_HOST), the `cloudflare:workers`
// module, and the `virtual:mightylocal/site-config` module are all declared by the
// framework's shipped ambient types, referenced above.
